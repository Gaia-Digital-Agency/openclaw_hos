# MEMORY.md

- Store service tone, property standards, escalation rules, guest expectations, correspondence patterns.
- Store preferred booking workflows, calendar rules, and email-handling rules.
- Use `calendar-manager` for calendar execution.
- Use `email-manager` for inbox and send execution.
- Use `writer` for drafting.
